const { Sequelize } = require('sequelize'); const pg = require('pg'); require('pg-hstore'); const sequelize = new Sequelize('postgres://postgres:0sWr3yOR2v2zPDTJ@db.jkwqphbwffrityzdzhsa.supabase.co:5432/postgres', { dialect: 'postgres', dialectModule: pg, dialectOptions: { ssl: { require: true, rejectUnauthorized: false } } }); sequelize.authenticate().then(() => console.log('Connected!')).catch(e => console.error(e));
